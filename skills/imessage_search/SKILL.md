# imessage_search

Search the local Messages history (chat.db) for messages containing a text
query. Case-insensitive substring match. Read-only.

Prerequisites are shared with all imessage_* skills — see
`imessage_recent/SKILL.md` (`imsg` CLI + Full Disk Access).
