# imessage_history

Read recent messages from a single iMessage/SMS conversation. Read-only.

Pass a `chat_id` from `imessage_recent`. Long texts are returned as-is;
attachment presence is reported as a count, not content.

Prerequisites are shared with all imessage_* skills — see
`imessage_recent/SKILL.md` (`imsg` CLI + Full Disk Access).
